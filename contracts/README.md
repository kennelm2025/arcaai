# Contracts (contract-first protocol, Blueprint S7.1)

API request/response JSON schemas, BentoML endpoint specs, and database schema
changes are committed HERE before either track builds against them.
First real contract: fraud scoring (B5). Contract changes require sign-off from
both tracks - in founder-led mode, that means a decision-record entry.
