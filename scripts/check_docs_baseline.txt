# check_docs baseline — findings accepted at a point in time.
#
# Empty. It was not: ten entries sat here on 2026-07-25, all of them
# artefacts of a resolution bug in check_docs.py, which resolved cited
# paths only from the repository root and so reported live documents as
# dead. Fixed the same day by trying repo-root, citing-file-relative and
# docs-relative in turn. The retirement those entries appeared to
# indict (DEC-0007) had in fact been carried out correctly.
#
# Kept because the mechanism is wanted: a finding may be accepted here
# rather than fixed, but never without a CL or DEC recording why, and
# each line is a to-do rather than a pardon.
#
# References under data/ are not checked at all — DVC manages those
# outputs, they are not in the git tree, and their presence depends on
# whether `dvc pull` has run.
