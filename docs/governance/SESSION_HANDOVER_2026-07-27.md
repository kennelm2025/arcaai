# SESSION HANDOVER — ArcaAI (2026-07-27)

*Supersedes SESSION_HANDOVER_2026-07-25b. **B7 entered and signed.**
Four PRs: #40 CL-23 to the register, #41 manifest divergence reporting
+ WS-E 53–55, #42 `policy_version`, #43 B7 entry sign-off.*

**First handover in the reduced shape.** Register numbering, gate
state, open CLs, structure and git state are not restated — they are
generated by `scripts/repo_manifest.py`. Reasoning is not restated
either: each item below names its commit, and the commit message
carries the argument in full. What is here is what neither can give —
what is open, what was observed, and what is next. One per day.

## Boot line

> Resume ArcaAI — B7 open, entry signed 2026-07-27 (`95dc8b0`).
> **NEXT: corpus manifest schema, time-versioned eligibility first;
> then the retrieval interface with `chromadb` in one adapter module
> (CF-1/B7-a); explicit pinned embedding function from the first line
> (B7_GATE §2.1).** Boot ritual: conda activate `arcaai` → git switch
> main → git pull --ff-only → git fetch --prune → `python
> scripts/repo_manifest.py --out D:\Downloads` and attach the manifest.
> **Read its Divergences section first** — four are known and expected
> (two Word lock files, the gif shadow, the `3W5uT (1).jpg` orphan);
> anything else is new. Docker Desktop by hand before
> `scripts/dev_up.cmd`. **Downloads is flat — `_staging` is retired.**

## What changed

- **CL-23 reached the canonical register** (`57b7f38`, PR #40). The
  ledger entry had been written to an untracked shadow copy of the
  changelog; neither it nor the CL-23 draft had ever been committed.
  Reconciled per DEC-0007, shadow deleted. **DRAFT pending panel
  review.**
- **WS-D §5's B8 hard trigger brought current** (`57b7f38`). It read
  "CL-17/19/20" while CL-21 and CL-22, filed the day the plan was
  ratified, both name that bundle in their own text. Now
  CL-17/19/20/21/22 with CL-23 provisional, under a dated currency
  note. No B8 criteria changed.
- **`scripts/repo_manifest.py` reconciles derived state instead of
  asserting it** (`e30403a`, PR #41). Divergences section,
  tracked/untracked marking, ahead/behind, shadow detection graded by
  content and commit status, hygiene flags. Fixing it surfaced three
  more instances of the same habit — see WS-E 53.
- **WS-E 53–55 entered** (`e30403a`); both standing-principle
  derivations extended; footnote added that a relative timestamp is not
  transcribable evidence.
- **`policy_version` declared in execution metadata** (`f32c830`,
  PR #42) — the substantive change. RAT-02 §4's declare-early rule
  applied; CL-23 gap 2's `metadata_extra` interim superseded. NULL
  until B8. Entered today **because the audit tables are still empty**,
  which is §4's own reasoning. Spec addendum items 3 and 4; item 4
  records `metadata_extra` itself, in the build since 25 Jul and named
  nowhere.
- **ADR-0010's evidence trail reaches the addenda** (`f32c830`) — one
  line. A drafted implementation-record section was discarded: DEC-0013
  rules these are recorded by addendum, not by edits to the ADR.
- **CL-18 cross-references CL-23 gap 1** (`f32c830`).
- **B7 entry signed** (`95dc8b0`, PR #43), before the first commit of
  the stage. §1 evidence left as at the date each criterion was met.
  Tracker row NOT STARTED → IN PROGRESS.

## Process changes adopted

The programme carries four overlapping registers, seven handovers in
four days, and a CL backlog whose intake exceeds its closure; B7 sat
entered-but-unstarted for two days while governance repaired
governance. Adopted as process, not as ratified decisions: **handovers
carry only what the manifest and the commit log cannot give** (this
document); **WS-E becomes a rules index over a raw append-only log**
rather than curated case histories, and the items 1–23 backfill rider
is retired rather than carried indefinitely; **one handover per day.**

Deferred to the DEC-0009 checkpoint: splitting the CL ledger into
gate-blocking and next-revision lanes, twelve of thirteen open CLs
being the second kind. Not cut and not up for cutting: the gate
documents and the CF-1 nominations.

## Open, in priority order

1. **`docs/governance/SESSION_HANDOVER_2026-07-25b.md` has an
   uncommitted working-copy edit (+24 −3), untouched all day** — the
   only dirty file and the only uncommitted governance record. Resolve
   before the next branch switch.
2. **Corpus manifest schema** — time-versioned eligibility first
   (B7_GATE §3): hardest item to retrofit, and what B9 replay needs.
3. **Manifest output default parked.** A 502-line version adding
   `ARCAAI_MANIFEST_OUT` was drafted and not landed; until it does, the
   boot command carries `--out D:\Downloads`.
4. **`_staging` retirement needs recording.** Item 44 established it as
   a control and WS-E 52's rules assume it.
5. **`Status:` headers for B1–B4 and B6 gate documents** — only B5 and
   B7 carry one, so five stages still derive state from the tracker
   rather than the document that owns it.
6. **Riders unchanged:** `decisions/` → `adrs/` rename; disk sprawl
   purge; CL-09 before external review; G10 reviewers, no date;
   `prompts/` scaffold at B8; coverage sources excluding `arcaai/`,
   revisit at B7 close; two Word lock files and the `~$*` gitignore;
   `3W5uT (1).jpg` wants renaming — it has no base file.

## Observed, not acted on

- **`GRANT_SQL` is duplicated in `tests/governance/conftest.py`** and
  applied from there, while its docstring says grants come from
  `sql/governance_grants.sql`. The grant is what makes append-only
  real, and the suite asserts against its own copy rather than the file
  CI and production apply. If they drift the suite stays green while
  the database differs. CL or `scripts/check_docs.py` candidate.
- **Node.js 20 deprecation on every ci-devops run** —
  `actions/checkout@v4` and `actions/setup-python@v5` forced onto
  Node 24. Bump both, or record as an accepted residual.
- **`api/` has its own `tests/` subdirectory** — a second test root,
  relevant to the coverage question parked to B7 close.
- **`archive/` and `diagrams/`** now visible in the manifest's code
  directories; neither shows untracked, so each is tracked or ignored.

## Regulatory watchlist

Unchanged from 25b: UK GDPR Articles 22A–22D in force since 5 Feb 2026;
SS1/23 scope precision; FCA audit-trail and human-in-the-loop guidance
expected during 2026 (RAT-12 trigger on publication); CTP designations
expected from HM Treasury during 2026.

## CI transcriptions (2026-07-27)

Per RAT-01 §3.1 — logs expire. Governance and tooling runs; **not B7
entry or exit evidence**, which the gate document carries itself.

- ci-docs #14 · pull_request · success · 17s · PR #40
- ci-devops #66 · push · success · 2m 29s · `57b7f38`
- ci-mlops #68 · push · success · 3m 36s · `57b7f38`
- ci-docs #15 · push · success · 15s · `57b7f38`
- ci-docs #16 · pull_request · success · 11s · PR #41
- ci-devops #67 · pull_request · success · 2m 15s · PR #41
- ci-docs #17 · push · success · 9s · `e30403a`
- ci-devops #68 · push · success · 2m 38s · `e30403a`
- ci-mlops #69 · push · success · 3m 26s · `e30403a`
- ci-docs #19 · push · success · 10s · `f32c830`
- ci-devops #70 · push · success · 2m 50s · `f32c830`
- ci-docs #20 · pull_request · success · 11s · PR #43
- ci-docs #21 · push · success · 8s · `95dc8b0`
- ci-devops #71 · push · success · 2m 46s · `95dc8b0`
- ci-mlops #71 · push · success · 3m 59s · `95dc8b0`

Local: `pytest tests\governance -q` — 27 passed, docker dev Postgres 16
under `arcaai_app`, before PR #42 was staged.

## Environment

Unchanged. `arcaai` conda env (py3.11.15). Docker Desktop manual start
→ `scripts/dev_up.cmd` (Postgres 16 + MLflow). `gh` CLI not installed;
GitHub web is the PR route. Governance suite **27 tests** (RAT-02 §7's
24 plus three parametrised cases added 27 Jul).
