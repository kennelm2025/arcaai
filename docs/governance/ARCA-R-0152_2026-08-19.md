# ARCA-R-0152, 19 August 2026 — return record, arc A-2026-08-19-03

**Provenance.** Originated as a Gmail draft, subject `ARCA-R-0152 [STAGED]`,
19 August 2026. A carriage copy was fetched verbatim and staged by the
coordinator as the Chair's pen on 21 August 2026, and transcribed here under
PROMPT 132R on the same date. **The Gmail draft is the original of record for
the 19 August staging act**; this file is the transcription, and where the two
disagree the draft governs for what was staged on that date.

**Transcribed, not authored.** The body below is the carriage copy verbatim.
Nothing is reworded, reformatted, corrected or abridged. Line endings are
normalised to LF, and the body is enclosed in a fence so its original
plain-text layout renders as written rather than being reinterpreted as
Markdown — the convention established at
`docs/governance/DEC-0018_RIDER_R1_2026-08-19.md`. The bytes between the fences
are the draft's own.

**What was left outside the fence, stated so the verbatim claim is exact.** The
carriage copy carries a one-line carriage header and the two record markers;
none of the three is part of the record and none is transcribed. The source
file also carries a UTF-8 byte-order mark on its header line and a single stray
carriage return on its closing marker line. Both are transport artefacts, both
sit outside the record body, and neither is carried here. **The body itself is
pure ASCII, LF-terminated, with no trailing whitespace on any line, so it
transcribes with zero normalisation** — the LF clause above is stated for
convention rather than because anything needed converting.

**Authority.** This is a return record, not a register. It consumed no register
number, and section 7 states so expressly. Where it and the registers disagree,
`DECISIONS.md` and `docs/governance/WS-E_INCIDENTS.md` win.

**Why it is in the tree at all.** Until this act **no ARCA-R record had ever
been committed to the repository** — the series appeared here only as citation,
in the incident register's cross-references. That is the gap recorded at
`CLAUDE.md` queue item 34 M2, the deliberative trail sitting outside the
evidence perimeter. This transcription closes the instance for this record; it
does not close M2, which is a mechanism and remains owed.

**Entry structure.** The fenced body is entry 1, as staged on 19 August. Entry 2
below the fence is a repository act of 21 August 2026 and forms no part of the
19 August draft.

---

```
ID: ARCA-R-0152
TS: 2026-08-19T19:19:09Z
RE: A-2026-08-19-03
ANSWERING PROMPT 152

STATUS=COMPLETE-DIAGNOSTIC-NO-MUTATION

>> HEADLINE, BECAUSE IT INVERTS THE ITEM'S OWN EXPECTATION: THE CRUX PROBE
>> Q1 PASSED. The conjunction rule predicted DENY and the guard allowed it.
>> By the bar this item set for itself - "the bar for claiming 'one
>> mechanism' is that the crux probe reproduces the P1/P3/P5 verbatim
>> deny" - THE UNIFICATION CLAIM IS NOT ESTABLISHED IN THE PRESENT TENSE.
>> And the reason is not that the hypothesis was silly: it is that WS-E 73
>> HAS BEEN FIXED since it was recorded, which neither R-0151 nor this
>> item's framing accounted for.

SCOPE HELD: read-only register reads and scratchpad writes only. No repo
file edited, no commit, no push, no PR, no register written, no number
taken. NO PROBE WAS RESHAPED, RE-WORDED OR RETRIED IN ALTERED FORM. Two
denials fired and both are recorded as data points; nothing was worked
around. NO REGISTER WRITE IS PROPOSED BY THIS ITEM.

IDEMPOTENCY GATE, run BEFORE any probe per instruction 9: a draft listing
for ARCA-R-0152 returned empty. Run after execution it would prove nothing.

RELEASE GATE: subject read ARCA-P-0152 [RELEASED]. Verified by a
DISCRIMINATING check rather than a permissive one - a subject query for the
RELEASED token returned the draft, and the negative control, the same query
for the STAGED token, returned EMPTY. A filter that returns a hit for both
states discriminates nothing. Body re-read at release and byte identical to
the staged read.

------------------------------------------------------------------------
1. BOOT
------------------------------------------------------------------------

Tree CLEAN. HEAD 5a8121c, origin identical. Rehash sweep GREEN, no
carve-out - it RAN, seven manifest versions, zero pinned rows, both
categories 0, all pins verified. Environment carried forward from earlier
this session (Python 3.11.15, envs\arcaai), stated as a carry-forward
rather than a fresh reading. Retrieval pre-flight NOT TRIGGERED. No
register precondition per instruction 2; anchor stands at WS-E next 77,
untouched by this item.

------------------------------------------------------------------------
2. SOURCE READ - WS-E 73 PROBE SET TRANSCRIBED BEFORE ANYTHING WAS RUN
------------------------------------------------------------------------

From the incident register on main, the WS-E 73 entry, under "Observed,
four commands, one variable", transcribed verbatim:

  1. ls -la <path>/.claude/agents/ EXECUTED
  2. git check-ignore -v .claude/agents/corpus-lister.md EXECUTED
  3. ls -la <path>/.claude/skills/ EXECUTED
  4. ls -la <path>/.claude/agents/ 2>&1 | head -3 REFUSED

WHAT IS NOT FULLY RECOVERABLE, recorded rather than reconstructed, per
instruction 4:

  (a) <path> IS ELIDED IN THE REGISTER. The entry writes the placeholder,
      not the absolute path, so the commands cannot be executed literally.
      The repository root was substituted. THIS IS A DECLARED DEVIATION
      FROM VERBATIM and is the only one.

  (b) THE ENTRY RECORDS RESULTS, NOT RAW OUTPUT. There is no captured
      stdout to compare against, so "reproduces" below means the
      pass/refuse OUTCOME matches, not that the bytes match.

>> A THIRD OBSERVATION, FOUND IN THE ACT OF TRANSCRIBING AND WORTH MORE
>> THAN THE RE-RUN IT PRECEDED. The entry's own heading says "four
>> commands, ONE VARIABLE". IT IS NOT ONE VARIABLE. Probe 4 differs from
>> probe 1 by TWO additions - the descriptor duplication 2>&1 AND the pipe
>> into head -3. WS-E 73's corrected diagnosis attributes the trigger to
>> 2>&1 while a pipe was introduced in the very same command. That is the
>> "true variable never isolated" error - and WS-E 73's own Family
>> paragraph names that error as the reason its FIRST diagnosis was wrong.
>> The correction repeated the methodological defect it was written to
>> correct. Q5 in Part B is what discriminates it, and it does.

------------------------------------------------------------------------
3. PART A - WS-E 73 VERBATIM RE-RUN
------------------------------------------------------------------------

A1 ISSUED: ls -la D:/ArcaAI-repo/arcaai/.claude/agents/
    ANSWERED: EXECUTED. Directory listing returned, two files.
    REPRODUCES the register's EXECUTED.

A2 ISSUED: git check-ignore -v .claude/agents/corpus-lister.md
    ANSWERED: EXECUTED. Exit code 1, NO guard refusal text.
    REPRODUCES the register's EXECUTED.
    >> DISTINCTION KEPT: exit 1 is GIT'S OWN answer, meaning the path is
    >> not ignored. It is NOT a guard denial. Per the WS-E 74 probe-spec
    >> lesson, a tool's own error and a guard result are different facts
    >> and collapsing them is how a table produces false rows.

A3 ISSUED: ls -la D:/ArcaAI-repo/arcaai/.claude/skills/
    ANSWERED: EXECUTED. Directory listing returned, eight entries.
    REPRODUCES the register's EXECUTED.

A4 ISSUED: ls -la D:/ArcaAI-repo/arcaai/.claude/agents/ 2>&1 | head -3
    ANSWERED: EXECUTED. Returned the first three lines of the listing.
    >> DOES NOT REPRODUCE. The register records this exact command as
    >> REFUSED with the write-deny text. It now runs.

PART A RESULT: THREE OF FOUR REPRODUCE. THE ONE THAT MATTERS - the only
refusal in WS-E 73's set - DOES NOT.

------------------------------------------------------------------------
4. PART B - CONJUNCTION-RULE CROSS-PROBES
------------------------------------------------------------------------

Scratchpad path elided as <SP> for readability; written in full in every
command as issued.

Q1 THE CRUX.
    ISSUED: grep -n ".claude/settings.local.json" .gitignore 2>&1
    PREDICTED by the conjunction rule: DENY.
    ANSWERED: PASSED. Returned "35:.claude/settings.local.json".
    >> PREDICTION FALSIFIED.

Q2 ISSUED: grep -n ".claude/settings.local.json" .gitignore
    PREDICTED: PASS. ANSWERED: PASSED. Returned the same line.
    Matches R-0151 P7.

Q3 ISSUED: grep -n "text=auto" .gitattributes 2>&1
    PREDICTED: PASS. ANSWERED: PASSED. Returned "2:* text=auto".

Q4 ISSUED: echo "<line containing .claude/settings.json>" >>
    "<SP>/q4.txt"
    PREDICTED: DENY. ANSWERED: DENIED. Verbatim: "HARNESS WRITE DENIED:
    this command writes into the permission and ceremony system -
    settings.json, hooks/, skills/ or agents/ under .claude/. There is no
    in-session route to this path under any mode or approval - that is the
    ruled posture (WS-E 69 fix item 1, 2026-08-14), not an oversight to
    work around. Draft the change outside the tree, have the operator
    install it at the operator's own terminal, then branch -> PR -> merge.
    Reading these paths is unaffected. When it loads: settings.json is
    read at session start, so an installed change loads on restart."
    IDENTICAL, word for word, to R-0151's P1, P3 and P5.

Q5 ISSUED: echo "<line containing .claude/settings.json>" | grep -c
    "settings"
    PREDICTED: OPEN. ANSWERED: PASSED. Returned "1".

GRID. protected string / token present / result.
  Q1 yes 2>&1 PASSED
  Q2 yes none PASSED
  Q3 no 2>&1 PASSED
  Q4 yes >> (append) DENIED
  Q5 yes | (pipe) PASSED

------------------------------------------------------------------------
5. ANALYSIS
------------------------------------------------------------------------

(i) DID THE WS-E 73 RE-RUN REPRODUCE, PROBE BY PROBE?
    A1 yes. A2 yes. A3 yes. A4 NO - and A4 is the entire substance of
    WS-E 73, the other three being controls that passed then and pass now.
    So WS-E 73'S RECORDED BEHAVIOUR NO LONGER REPRODUCES.

    WHY, AND THIS IS NOT A MYSTERY. The item-42 fix spec named exactly
    this: "F2: stop treating 2>&1 as a write construct; distinguish a file
    redirection from a descriptor duplication, or the protected-path
    write-deny keeps firing on read-only commands." F2 merged at PR #143
    on 2026-08-19; WS-E 73 was observed 2026-08-17, before it. A4 passing
    is THE F2 FIX WORKING AS DESIGNED.

    >> THIS IS AN INDEPENDENT CONFIRMATION OF F2, AND IT IS WORTH MORE
    >> THAN THIS ITEM'S OWN QUESTION. Item 42's discharge asserted "F2 is
    >> proven in both directions: a read carrying a descriptor duplication
    >> executes, and a genuine write to a protected path still refuses."
    >> That was established by the item-42 probe set. Parts A and B here
    >> re-establish it from OUTSIDE that set, by a different route, with a
    >> different purpose - A4 and Q1 and Q3 for the read direction, Q4 for
    >> the write direction. An independent path to the same result is
    >> stronger than a repetition of the original one.

(ii) DID Q1 REPRODUCE THE P1/P3/P5 VERBATIM DENY - THE CRUX?
    NO. Q1 PASSED. The conjunction rule as stated in R-0151 predicted
    DENY and was WRONG for the descriptor-duplication token.

(iii) ONE MECHANISM OR TWO? THE ANSWER SPLITS BY TENSE, AND THE SPLIT IS
    THE WHOLE ANSWER.

    PRESENT TENSE: NOT ONE MECHANISM, and the claim is not merely
    unproven but false as posed. WS-E 73's trigger DOES NOT EXIST any
    more; WS-E 76's does. Two things cannot be one live mechanism when one
    of them has been repaired. This item's stated bar is not met, and it
    should not be finessed - the crux probe passed.

    HISTORICAL: ALMOST CERTAINLY ONE RULE, SUBSEQUENTLY NARROWED. Before
    F2, the write-detection treated the ">" character as a write construct
    WHEREVER it appeared, including inside 2>&1; combined with a protected
    path string that fired the deny. That is precisely the conjunction
    R-0151 isolated. F2 did not remove the rule; it NARROWED THE TOKEN SET
    the rule recognises, excluding descriptor duplication while leaving
    genuine write redirection in. Q4 and Q1 are the two halves of that
    narrowing, observed directly: append DENIES, descriptor duplication
    PASSES.

    So WS-E 73 and WS-E 76 are best read as THE SAME RULE OBSERVED AT TWO
    POINTS IN TIME, either side of a fix - not as two rules, and not as
    one live mechanism.

    CONFIDENCE, STATED HONESTLY. The present-tense finding is
    PROBE-ESTABLISHED: nine probes, two denials, verbatim text captured.
    The historical finding is INFERENCE and cannot be otherwise - the
    pre-F2 guard cannot be probed from here, and no probe run today can
    reach it. It rests on three legs: F2's fix spec names this exact
    behaviour; A4 no longer refuses; and Q1-versus-Q4 shows precisely the
    file-redirection-versus-descriptor-duplication distinction F2
    specified. Strong, converging, and still inference. IT SHOULD NOT BE
    WRITTEN INTO ANY REGISTER AS A PROBE RESULT.

    THE FAMILY BOUNDARY, NOW MAPPED. The trigger recognises WRITE
    REDIRECTION - ">", ">>" and the heredoc form - and EXCLUDES descriptor
    duplication (Q1, Q3, A4) and pipes (Q5). That is a coherent boundary
    and looks deliberate rather than accidental, being exactly what F2
    asked for.

    >> AND IT RETROSPECTIVELY RESOLVES THE WS-E 73 CONFOUND AT SECTION 2.
    >> Probe 4 changed two variables at once, so the entry's attribution
    >> to 2>&1 rather than to the pipe was not isolated by its own
    >> evidence. Q3 and Q5 now separate them under the current guard, and
    >> F2's success - targeting 2>&1 and demonstrably fixing the
    >> behaviour - is independent evidence the attribution was RIGHT. WS-E
    >> 73's diagnosis was correct on under-isolated evidence. Correct, but
    >> not rigorously established at the time; the rigour arrived later,
    >> from the fix working.

(iv) WHAT REMAINS UNCHARACTERISED. No extrapolation beyond the nine probes
    actually run.

    (a) EVERY DENIAL EVER OBSERVED USED A SETTINGS PATH. P1, P3, P5 and Q4
        all used .claude/settings.json or .claude/settings.local.json. NO
        DENY HAS EVER BEEN OBSERVED WITH A hooks/, skills/ OR agents/
        STRING. A4 carried an agents/ string but with tokens now known to
        be non-triggering, so it does not test this. WHETHER THE
        PROTECTED-STRING SET COVERS ALL FOUR FAMILIES OR ONLY SETTINGS IS
        UNTESTED - and the deny text names all four, which makes the
        assumption tempting and still unevidenced.
    (b) PRE-F2 BEHAVIOUR IS UNREACHABLE. The historical reading at (iii)
        cannot be probed and will remain inference.
    (c) POWERSHELL UNTESTED. Every probe ran on the Bash tool. WS-E 64 is
        the standing reminder that coverage is evidenced per tool.
    (d) INTERPRETER-INTERNAL WRITES UNTESTED - a write performed inside
        python or similar, carrying no shell redirection token at all.
    (e) STRING POSITION NOT ISOLATED - content in every denied probe;
        filename argument, flag value and comment untested.
    (f) THE GUARD'S CODE WAS NOT READ. This characterises BEHAVIOUR from
        outside.

CONTRADICTION CHECK, per instruction 7.

  AGAINST WS-E 73: A4 CONTRADICTS THE ENTRY'S PRESENT-TENSE READING. The
  entry records that command as REFUSED and states "NOT FIXED HERE",
  pointing at a fix spec. It carries NO note that the fix subsequently
  landed. A reader of the incident register alone would conclude the
  defect is live; it is not. THIS IS A REGISTER-STATE FINDING: WS-E 73
  does not record its own discharge by F2 at PR #143. It is not a
  contradiction of what the entry observed on 2026-08-17, which stands.

  AGAINST WS-E 76: NO CONTRADICTION. Its description holds exactly. Q4
  EXTENDS it (append is in family, which the entry left untested) and Q1
  and Q5 BOUND it.

  ONE REFINEMENT, SHORT OF CONTRADICTION. WS-E 76 says of itself: "this is
  not item 73 recurring and not item 73's corrected diagnosis returning;
  it is a third trigger in the same false-RED family." On this evidence
  "third trigger" is stronger than warranted - the better reading is the
  SAME RULE with the descriptor-duplication token removed by F2. The
  entry's "same false-RED family" is right; "third trigger" was a
  reasonable reading when no probe set existed and is now superseded.

  NO REGISTER WRITE IS PROPOSED. Unifying the entries, amending WS-E 76 to
  carry R-0151's characterisation, or noting WS-E 73's discharge, are
  LATER GOVERNED ACTS requiring a chair ruling, and are not this item's.

------------------------------------------------------------------------
6. PERMISSION-ASK LEDGER - ROUTE 1, AND HELD
------------------------------------------------------------------------

>> METHOD AND ITS LIMIT. Observable events ONLY: denials, blocks,
>> refusals. A call that succeeded under a grant and one that succeeded
>> because a human approved a prompt are INDISTINGUISHABLE from this
>> vantage. "No denials observed" NEVER means "no ask fired". WS-E 75.

BOOT: git status, rev-parse, rehash sweep. NO DENIALS OBSERVED.
SOURCE READ: register search and file read. NO DENIALS OBSERVED.
PART A: four probes. NO DENIALS OBSERVED - including A4, which the
  register records as refused. A2's exit 1 was git's own and is not a
  denial.
PART B: five probes. ONE DENIAL OBSERVED - Q4, returning the guard's
  verbatim write-deny text, reproduced in full at section 4. A data point,
  not an obstacle.
R-DRAFT STAGING: queue reads and this draft's creation. NO DENIALS
  OBSERVED.

>> Q4 is the deny-shaped half and the eight passes are the allow-shaped
>> half, so the guard is evidenced LIVE and DISCRIMINATING this session.
>> That is a statement about the guard and not about Amendment 5's grant
>> architecture, which is unaffected by this item.

------------------------------------------------------------------------
7. WHAT THIS ITEM DELIVERS, AND WHAT IT DOES NOT
------------------------------------------------------------------------

DELIVERS, and item (c) of the owed list IS DISCHARGED: the unification
question is answered. NOT one mechanism in the present tense - the crux
probe passed and the bar was not met. Almost certainly one rule
historically, narrowed by F2, stated as inference and labelled as such.
The redirection-family boundary is mapped: append IN, descriptor
duplication OUT, pipe OUT. Two by-products outrank the answer in places -
an independent confirmation of the F2 fix, and the discovery that WS-E
73's own "one variable" probe carried two.

DOES NOT DELIVER, none in scope: any register write or amendment; any
guard or matcher change; any test of the six uncharacterised dimensions;
any characterisation of the pre-F2 guard, which is unreachable.

OWED-FORWARD, carried and NOT discharged here:
  (b) the ruling on whether and how to amend WS-E 76 - which this
      diagnostic informs and does not perform. It now has more to inform
      it: the append extension, the family boundary, the "third trigger"
      refinement, AND the separate question of whether WS-E 73 should
      record its own discharge.
  (d) the auto-mode classifier-block placement recommendation from
      ARCA-R-0150 section 6, queue item 49 standing-risk family.
  the chair question, parked with the WS-E 76 amendment, of whether the
      safe-delete ask is intended behaviour or matcher breadth.
  NEW, arising here: whether WS-E 73's entry should carry a discharge note
      recording the F2 fix. Raised, not acted on.

REGISTERS: untouched. No number taken. Anchor stands at DEC next 0019,
ADR next 0011, CL next 31, WS-E next 77.

ARC A-2026-08-19-03 REMAINS OPEN. Repository on main at 5a8121c, tree
clean, no branch residue.

- Entry 1 staged by CC. Single CC instance; CC-ID omitted per A4.1.
- Dotted filenames avoided in prose per extended F-R1. The protected path
  strings in sections 2, 3 and 4 are UNAVOIDABLE and DELIBERATE: they are
  the probes' independent variable, per the R-0151 precedent and this
  item's own scope note.
```

---

## Entry 2 — the fold: operator half of the ask ledger (appended 2026-08-21)

**A repository act under PROMPT 132R, appended per the A4.5 convention: entry 1
above is never overwritten.** Entry 1 recorded what the executor could observe.
This entry records what only the operator could. The two are kept apart because
they are different vantage points, and neither can see the other's class of
event — the standing WS-E 75 limit, which entry 1 section 6 states against
itself before this half arrived to complete it.

**--- OPERATOR HALF, verbatim (Chair, coordinator chat, 19 Aug 2026) ---**

> "Operator ask-ledger halves: PROMPT 152 - none fired. PROMPT 153 -
> none fired."

**--- Coordinator note carried with it (ARCA-D-OPERATOR-HALVES-152-153):**

> PROMPT 152: executor observed exactly one denial (Q4, predicted,
> verbatim deny) and zero asks surfaced to the operator. PROMPT 153: a
> register-write PR ran branch-to-PR-open with zero asks surfaced.
> Both runs now carry complete two-half evidence. ---

**What this closes for this record, precisely.** Entry 1 section 6 recorded one
denial — Q4, returning the guard's verbatim write-deny text — and no denials
anywhere else, while stating its own limit in terms: *"No denials observed"
NEVER means "no ask fired"*. The operator half now supplies exactly what that
vantage could not reach: **no ask fired**. The run's ask ledger is complete in
both halves, which is what the two-halves pattern exists to produce.

**What it does not close.** The completion is of this run's ledger, not of
WS-E 75. That entry records a structural gap — no vantage point both triggers
and observes a live ask — and it is untouched by an operator reporting what
their screen did or did not show. This fold is the workaround the gap requires,
not evidence against the gap.
