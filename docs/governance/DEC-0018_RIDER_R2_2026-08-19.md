# DEC-0018 Rider R2, 19 August 2026 — source document

**Provenance.** Fetched from Gmail transport on 2026-08-19 and transcribed
here. Draft subject, quoted exactly:

> ArcaAI — DEC-0018 RIDER R2: unattended operation model (queue protocol, CC
> IDs, arc IDs, auth once-and-for-all) — 19 Aug 2026

**Transcribed, not authored.** The body below is the fetched draft verbatim.
Nothing is reworded, reformatted, corrected or abridged, including text the
subsequent ruling superseded. Line endings are normalised to LF; the body is
enclosed in a fence so its original plain-text layout renders as written
rather than being reinterpreted as Markdown. The bytes between the fences are
the draft's own.

**Authority.** This is a source document, not a register. Where it and
DEC-0018 as ruled disagree, **`DECISIONS.md` DEC-0018 and
`docs/governance/DEC-0018_A6_CORRECTION_2026-08-19.md` win.**

## Provenance note — A6 and E2 below are the SUPERSEDED PREMISE

**This is the one part of this document a reader must not take at face
value.** Amendment A6 below, and its entry condition E2, rest on a premise
that a read of `.claude/settings.json` the same evening found already
answered. The correction file governs, and the divergence is specific:

- **A6.1(i) treats allow-pre-empts-guard as NOT assumed, to be settled by
  probe.** It was already settled — proven 2026-08-11 and recorded in that
  file's own standing-constraint note. Not an open question at the time A6
  was drafted.
- **A6.2's E2 probe was CANCELLED UNRUN.** It never went green. E2 is
  therefore **SUPERSEDED, not satisfied**, and A6.1's resolution of item 27
  Part B's grounds — expressed as contingent on E2 green — cannot be read as
  having triggered.
- **A6.3's "on E2 green, chair applies the Part B widening" never fires.**
  The settings-side widening is **struck as the mechanism**, because an allow
  rule pre-empts the guard and the guard is the only component positioned to
  write A2's log row. Envelope resolution moves inside the guard instead, as
  an F5-class amendment owed as its own governed act (queue item 48).

**What survives A6 untouched:** A6.4's exhaustive list of what the chair still
touches, and the standing rule that merge to main is outside every allow-list
with M3 branch protection backing it server-side. Those were never contingent
on E2.

**A5 is the part of this rider already discharged.** `A-2026-08-19-01` was
recognised as first use and `docs/governance/ARC_REGISTER.md` now exists, as
A5.2 proposed.

**Why it is in the tree at all.** DEC-0018 adopts this rider "in full form",
and until this commit no reader could read A3 through A6 in their own words —
the gap recorded at `CLAUDE.md` queue item 34 M2.

---

```
ARCAAI — DEC-0018 CANDIDATE RIDER R2: UNATTENDED OPERATION MODEL
Prepared 19 Aug 2026 (chat lane, coordinator = this Claude session). Status: UNDELIVERED to CC. PROPOSED — attaches to the DEC-0018 candidate (17 Aug) and Rider R1 (19 Aug); if the number dispute assigns 0018 elsewhere, R2 follows the delegation/envelope candidate to its number. Supersedes nothing; extends R1. Registers are the authority where this document and the repo disagree.

CHAIR INTENT (stated 19 Aug, post round-trip test): CC runs unattended. The chair retains ONE per-transaction authority: merge to main. All other chair involvement moves to the Gmail queue — rulings requested and given as drafts, prompts passed with IDs and timestamps, reviewable from a mobile phone. Multiple CC instances must be distinguishable.

======================================================================
AMENDMENT A3 — GMAIL QUEUE IN THE DEFAULT TOOLSET (extends R1 A1)
======================================================================
A3.1 Add to the §4.2a standing toolset:
  (vii) Gmail queue operations, scoped to the ARCA-* subject namespace only:
        draft search/read of ARCA-P/ARCA-R/ARCA-Q/ARCA-D series;
        draft CREATION in the ARCA-R (results) and ARCA-Q (ruling
        requests) series. No send. No draft outside the ARCA-* namespace
        may be read, created, edited, or deleted. Chair's other mail is
        out of scope terrain, structurally.
A3.2 Basis: the 19 Aug round-trip test (PROMPT 144/145) proved the loop live and proved the blocker — one reflex decline at the per-call permission gate stalled the queue. Unattended requires these calls pre-approved; the namespace scoping keeps the mailbox closed while the queue is open.

======================================================================
AMENDMENT A4 — QUEUE MESSAGE PROTOCOL (subjects, IDs, timestamps)
======================================================================
A4.1 Subject grammar, all queue traffic:
  ARCA-<TYPE>-<NNNN> [<STATE>] [<CC-ID>]
  TYPE:  P = prompt (chair/coordinator → CC)
         R = result (CC → chair)
         Q = ruling request (CC/coordinator → chair)
         D = ruling decision (chair → CC; may be the Q draft edited in place)
  NNNN:  the prompt tally number for P/R; a Q takes the next tally number
         when raised and its D answers under the same number.
  STATE: STAGED (not executable) | RELEASED (executable — chair's rename
         is the release act, D1 unchanged) | ANSWERED (Q resolved).
  CC-ID: CC1, CC2, ... — MANDATORY on every P/R when more than one CC
         instance exists; a P addressed [CC2] is invisible to CC1.
A4.2 Body header, first three lines of every queue item:
  ID: ARCA-<TYPE>-<NNNN>
  TS: <ISO-8601 with timezone, written at creation>
  RE: <arc ID this item serves, or "unassigned">
  The Gmail draft date is corroboration; the TS line is the timestamp of
  record (draft dates shift on edit — the release rename must not
  overwrite when the item was authored).
A4.3 Ruling-by-queue flow: CC hits a Class C boundary mid-envelope → stages ARCA-Q-<n> [STAGED] stating the question, the options, and its recommendation → continues any work not gated on the answer → chair answers from anywhere by editing the draft to [ANSWERED] with the ruling appended under a "RULING:" line → CC's next poll consumes it. The Q/D pair is folded into the repo at the next session's fold-in PR, so the register never depends on Gmail as permanent storage.

======================================================================
AMENDMENT A5 — ARC IDENTIFIERS (regularises the 19 Aug first use)
======================================================================
A5.1 Arc ID grammar: A-YYYY-MM-DD-NN, assigned at arc OPEN (not close), with an OPEN timestamp (ISO-8601) and, at close, a CLOSE timestamp — both recorded in the arc record. A-2026-08-19-01 stands as first use; its record gains timestamps retroactively at next touch, marked as reconstructed.
A5.2 Every envelope, queue item (RE: line), and cost row cites its arc ID. The arc-register home is the item 41 question (widened 19 Aug) — the identifier scheme needs a register file; proposed docs/governance/ARC_REGISTER.md, one line per arc: ID · name · lane · OPEN ts · CLOSE ts · PRs · cost rows.

======================================================================
AMENDMENT A6 — AUTH, ONCE AND FOR ALL (resolves item 27 Part B)
======================================================================
A6.1 Ruling this rider RESOLVES Part B's three grounds:
  (ii) "no ruling ever made" — this is the ruling;
  (iii) "ask-tier guard-proven-only" — answered by R1 A2: in-envelope
        asks become ALLOW-AND-LOG, reviewed at stop points, so no ask
        depends on unprovable human surfacing;
  (i)  "allow-pre-empts-guard" — NOT assumed. Entry condition E2 below.
A6.2 ENTRY CONDITION E2 (blocking, like E1 was): before any settings.json widening is applied, a two-row probe proves the PreToolUse hook still fires on an allow-listed tool: (row 1) allow-list a harmless command class, issue a guard DENY-tier string through it, expect the deny verbatim; (row 2) issue an ASK-tier string, expect the ask emission guard-side. If allow silences the hook, Part B stays HELD and this amendment's auth half is void — the guard is the control, and nothing may quietly stand in front of it.
A6.3 On E2 green: chair applies the Part B widening at the operator terminal (Class C act, drafted outside the tree per the F1-F4 route), scoped to the R1 A1 + A3 toolset. Merge to main remains outside every allow-list — the ask/deny on merge is never widened, and M3 branch protection backs it server-side regardless.
A6.4 What the chair still touches, exhaustively: envelope approval at dispatch (can be a released ARCA-P from a phone); ARCA-Q rulings (from a phone); merge to main (SO-1, bare trigger lawful); elevated OS operations; frozen-path amendments. Nothing else.

======================================================================
CHAIR DECISIONS SOUGHT (one sitting, in order)
======================================================================
Q-R1. Assign the DEC number (delegation/envelope vs dashboard — both claim 0018).
Q-R2. Rule the delegation/envelope candidate + R1 + R2, full or reduced form per Footnote F1.
Q-R3. Authorise the E2 probe as the next session's first act (it is small, read-shaped in effect, and everything unattended gates on it).

— Prepared by Claude (coordinator lane), 19 Aug 2026. Held outside the repo per standing convention. Next prompt tally: 146 (144/145 consumed by the round-trip test).
```
