# ArcaAI — Morning Run-Sheet, Wednesday 12 August 2026

Source: Gmail draft "ArcaAI Accelerator Pack v3 — install brief" (11 Aug evening).
Governance note carried forward: nothing below creates a rule. Draft items await Mike's ruling.
Precedence rule for this session: **the zip pack is the artefact**. Any file in this bundle that
overlaps a pack file is a fallback/alignment reference only — on conflict, the pack wins, and a
conflict is a transcription defect to correct, never a reinterpretation.

## Hard precondition (do not skip)
**Test-database isolation fix before any background test cadence.** The suite currently wipes the
live audit database on startup. No corpus-lister fan-out, no commissioning runs, nothing on a
cadence until this is fixed and verified.

## Sequence

**0. Download & stage (Mike, ~2 min)**
- Download `arcaai-accelerator-pack-v3.zip` from the Gmail draft; unzip at the repo root's staging area.

**1. Install (Claude Code, Mike's terminal)**
- `skills/*` → `.claude/skills/`; `agents/*` → `.claude/agents/`.
- Restart Claude Code if `.claude/agents/` is new.

**2. Alignment pass FIRST (Claude Code lead)**
- Lead reads the ruled permission policy (PR #95).
- Reconcile both agents' tools lists to the tiering vocabulary.
- Mismatches are fixed **in the agent files** — the ruled policy is never bent to fit them.
- Also reconcile against the fallback files in this bundle if used; pack wins on conflict.

**3. Land the pack as one PR**
- Next free PR number, commit message via message file (commit-hygiene).
- Same PR includes: Mobile Ruling Protocol setup (issue template, labels, `ruling-briefs/` dir)
  and the Boundary Tagging mods (tag vocabulary + `contract-seed.md`).

**4. Ruling (Mike, ~60 seconds)**
- Rule the build-first right-of-way DEC candidate. **Wording is in the pack README — rule from
  there, not from any restatement.** Number allocated only at ruling, read from the live register
  (DEC next was 0016 as of last recorded state — read live, never assume).

**5. Single-lane work: test-database isolation fix (Claude Code)**
- Acceptance per check-method: run the suite **twice**, then verify by reading the ACTUAL audit
  records that pre-existing entries survived — not the suite's own report.
- Any repair requiring elevation is Mike's, at his terminal, re-verified from a non-elevated shell.

**6. Open lane one — the test runner**
- Builder + test-author (spec-blind) in parallel; corpus-lister fan-out in background
  (only after step 5 is verified).
- Lane-close: rule spec gaps → one clean commissioning pass → one Formal Execution run from a
  non-elevated shell in Mike's session → merge via ruled PR.

## Draft items awaiting ruling (not rules until ruled)
1. Build-first right-of-way DEC candidate (step 4 — pack README wording).
2. Mobile Ruling Protocol (three-tier: issue card / ruling brief / panel-on-demand; `RULED:` token;
   desk-only label; never-idle rule).
3. Boundary Tagging + contract-seed accumulation (carry on, tag, accumulate; stop-and-spec trigger
   = first app team spin-up).
4. Strategic Staging Note → unnumbered ADR-0011 candidate (Agentic Topology, reserved) and possible
   ADR-0009 extension. Candidate seed drafted in this bundle for review, not for adoption as-is.

## What was prepared claude.ai-side this morning (this bundle)
- This run-sheet.
- `ruling-request.md` — issue template (fallback if not in pack).
- `ruling-briefs/README.md` — briefs directory convention (fallback).
- `contract-seed.md` — skeleton with tag vocabulary (fallback).
- `adr-0011-candidate-agentic-topology-seed.md` — unnumbered candidate draft from the Staging Note.
