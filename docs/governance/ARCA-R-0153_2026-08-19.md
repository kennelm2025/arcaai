# ARCA-R-0153, 19 August 2026 — return record, arc A-2026-08-19-03

**Provenance.** Originated as a Gmail draft, subject `ARCA-R-0153 [STAGED]`,
19 August 2026. A carriage copy was fetched verbatim and staged by the
coordinator as the Chair's pen on 21 August 2026, and transcribed here under
PROMPT 132R on the same date. **The Gmail draft is the original of record for
the 19 August staging act**; this file is the transcription, and where the two
disagree the draft governs for what was staged on that date.

**Transcribed, not authored.** The body below is the carriage copy verbatim.
Nothing is reworded, reformatted, corrected or abridged. Line endings are
normalised to LF, and the body is enclosed in a fence so its original
plain-text layout renders as written rather than being reinterpreted as
Markdown — the convention established at
`docs/governance/DEC-0018_RIDER_R1_2026-08-19.md`. The bytes between the fences
are the draft's own.

**What was left outside the fence, stated so the verbatim claim is exact.** The
carriage copy carries a one-line carriage header and the two record markers;
none of the three is part of the record and none is transcribed. The source
file also carries a UTF-8 byte-order mark on its header line and a single stray
carriage return on its closing marker line. Both are transport artefacts, both
sit outside the record body, and neither is carried here. **The body itself is
pure ASCII, LF-terminated, with no trailing whitespace on any line, so it
transcribes with zero normalisation** — the LF clause above is stated for
convention rather than because anything needed converting.

**Authority.** This is a return record, not a register. It consumed no register
number, and says so in both of its entries. Where it and the registers disagree,
`DECISIONS.md` and `docs/governance/WS-E_INCIDENTS.md` win. Note that the record
is the return leg for the act that landed the WS-E 76 amendment and the WS-E 73
discharge note; the register entries themselves are the authority on what those
edits say, and this record is the authority on how the act was conducted.

**Why it is in the tree at all.** Until this act **no ARCA-R record had ever
been committed to the repository** — the series appeared here only as citation,
in the incident register's cross-references. That is the gap recorded at
`CLAUDE.md` queue item 34 M2, the deliberative trail sitting outside the
evidence perimeter. This transcription closes the instance for this record; it
does not close M2, which is a mechanism and remains owed.

**Entry structure, and this record already had two.** The fenced body carries
entry 1 (at PR-open) and entry 2 (post-merge close-out), both as staged on
19 August, under the A4.5 convention that entry 1 is never overwritten. **Entry
3 below the fence is a repository act of 21 August 2026** and forms no part of
the 19 August draft. Entry 2's own closing list names the operator half as
STILL OWED at item (c); entry 3 is what discharges it.

---

```
ID: ARCA-R-0153
TS: 2026-08-19T19:33:59Z
RE: A-2026-08-19-03
ANSWERING PROMPT 153

>> TWO ENTRIES, per A4.5. Entry 1 is UNALTERED; its STATUS was true when
>> written and is SUPERSEDED by entry 2. Per F-R2 the body STATUS governs.

========================================================================
ENTRY 1 - AT PR-OPEN (2026-08-19T19:33:59Z). UNALTERED.
========================================================================

STATUS=AT-PR-OPEN

PR: 152 (number is the record per F-R1). Opened and STOPPED THERE.

IDEMPOTENCY GATE, run BEFORE execution per instruction 7: a draft listing
for ARCA-R-0153 returned empty. Ordering matters - run after execution it
would prove nothing, the F-Q4 lesson.

RELEASE GATE: subject read ARCA-P-0153 [RELEASED] verbatim at fetch. This
item arrived already released rather than staged-then-renamed, so no
staged-versus-released comparison was possible and none is claimed.

1. BOOT AND PRECONDITION

Tree CLEAN. HEAD 5a8121c, origin identical. Environment carried forward
from earlier this session (Python 3.11.15, envs\arcaai), stated as a
carry-forward rather than a fresh reading. Retrieval pre-flight NOT
TRIGGERED - the scope is two documentation files.

PRECONDITION, BOTH LIMBS CHECKED AND BOTH MET:
  (i) the incident register contains BOTH a WS-E 76 entry and a WS-E 73
      entry - confirmed by a count of entry-heading matches returning 2.
  (ii) the anchor reads WS-E next 77 - confirmed from a manifest
      REGENERATED AT THIS HEAD rather than cited from the boot manifest,
      as the item required. DEC next 0019, ADR next 0011, CL next 31,
      divergences 0.

2. NUMBERING TREATMENT - THE CONVENTION READ AND CITED, NOT INVENTED

RULE FOLLOWED: the project instructions file makes the governance review
change log CANONICAL FOR CL ITEMS, and that file's own header defines them
as "changes the review requires" - governance-review work items carried in
their own ledger. Appending dated amendment and discharge blocks to
existing incident-register entries, and adding a standing-risk note, is not
a review-required change item.

DETERMINATION: NO CL NUMBER IS CONSUMED. No new WS-E number is taken.

CORROBORATED BY PRECEDENT rather than by reasoning alone: today's PRs 142
through 151 carried a WS-E 74 discharge note, a WS-E 75 raise and a WS-E 76
raise, and the CL anchor stood unmoved at highest 30 throughout.

VERIFIED MECHANICALLY: the manifest was regenerated AFTER the edits and
reads WS-E next 77 and CL next 31, both unchanged, divergences 0. A stray
numbered entry would have moved the WS-E anchor and this check would have
shown it.

>> AN ERROR MADE AND CORRECTED WITHIN THE ACT, RECORDED RATHER THAN
>> QUIETLY FIXED. The first draft of EDIT 1 appended a placeholder line
>> beginning with a number, intended as a navigational anchor. That would
>> have created a bogus numbered entry in a register whose numbering the
>> manifest scanner parses off exactly that shape - the same mechanism that
>> makes a DEC-numbered file under the ADR directory silently consume an
>> ADR number. It was removed immediately, before any battery ran and
>> before the commit. The post-edit anchor check above is what would have
>> caught it had it survived, and it is recorded because a control that was
>> not needed is still worth knowing about.

3. THE THREE EDITS

EDIT 1 - WS-E 76 AMENDMENT. Appended as a dated block; original text
UNALTERED, with the governing rule stated: the block governs on trigger
behaviour, the entry above remains the record of what was observed. All six
required elements carried - (a) the conjunction trigger, neither half
sufficient, one string enough; (b) the family boundary, append IN,
descriptor duplication OUT, pipes OUT; (c) the destination never consulted;
(d) the instrument asymmetry, coverage differing by INSTRUMENT rather than
by ACT; (e) the "third trigger" refinement framed as a refinement of the
reading and NOT a contradiction of what was observed, "same false-RED
family" preserved; (f) the settings-path caveat preserved in force.

EDIT 2 - WS-E 73 DISCHARGE NOTE. Appended, original text UNALTERED. States
the discharge by F2 at PR 143 and why the note is needed - the entry ends
on "NOT FIXED HERE" with no successor, so the register alone misleads.
Evidence is the entry's own four probes re-run. Carries the method footnote
against the entry's own rigour.

THE HISTORICAL UNIFICATION IS RECORDED AS LABELLED INFERENCE, per the
ruling's constraint: only in the discharge note, marked INFERENCE, carrying
its three legs, and stating explicitly that the pre-fix guard cannot be
probed from any present vantage. The not-one-live-mechanism finding is
stated first and separately, as established.

EDIT 3 - CLAUDE.md ITEM 49. The classifier-block observation added beside
the MCP-matcher coverage fact. Stated as harness-level and NOT a governed
control, with the placement reasoning. The item-49 rationale is made
explicit: the unattended model now faces TWO OPPOSITE FAILURE SHAPES, and
neither is reachable by the envelope mechanism.

4. BATTERY, as the terminal printed it

  precondition - manifest regenerated BEFORE the act; WS-E next 77; both
                   entries present. MET.
  git diff --stat FIRST and NON-EMPTY - 2 files, 131 insertions
  check_docs - "Checked 176 markdown files. No findings."
  lint - "All checks passed!"
  pytest - NOT RUN. Zero .py files touched. Recorded as not-run
                   WITH ITS REASON, never as green.
  attribution - asserted against the FULL PRINTED BODY of the single
                   commit. No line asserts co-authorship. Body: e299d42.
  POST-ACT ANCHOR RE-READ - WS-E next 77, CL next 31, divergences 0.

FILES CHANGED, 2 files, +131:
  - the incident register under the governance directory +112
  - the project instructions file at repository root +19

COMMIT: single. Provenance did not require a split - all three edits are
executor-authored, unlike the PROMPT 149 case. e299d42.

5. PERMISSION-ASK LEDGER - ROUTE 1, AND HELD

>> Observable events ONLY. "No denials observed" NEVER means "no ask
>> fired". WS-E 75.

BOOT AND PRECONDITION: NO DENIALS OBSERVED.
CONVENTION READ: NO DENIALS OBSERVED.
BRANCH AND THE THREE EDITS: NO DENIALS OBSERVED.
  >> POSITIVE RESULT: the item warned that a denial on a register write
  >> ABOUT these paths would itself be a finding, the edits necessarily
  >> containing protected path strings. NO SUCH DENIAL OCCURRED -
  >> consistent with the P6 result and a second independent confirmation
  >> of the instrument asymmetry the amendment records.
COMMIT-MESSAGE AUTHORING: written with the file-write instrument from the
  outset. NO DENIALS OBSERVED.
BATTERY, COMMIT, PUSH, PR: NO DENIALS OBSERVED.

6. OWED, AND CARRIED FORWARD

FOR THE CHAIR, none blocking a B7 pivot:
  (a) THE SAFE-DELETE-ASK QUESTION. Recommended: a one-shot READ-ONLY
      matcher read. Three attested windows show the ask fires; none
      determines why.
  (b) WHETHER TO UNIFY THE WS-E 73 AND 76 ENTRIES STRUCTURALLY. This item
      deliberately did NOT; it cross-references both ways. On present
      evidence unification would also be premature - the entries are one
      rule at two points in time, and collapsing them would lose the
      before-and-after that makes the F2 fix legible.

FOR THIS DRAFT:
  (c) Entry 2, the post-merge close-out per A4.5.
  (d) The operator half of this run's ask ledger.

REGISTERS AT PR-OPEN: NO NUMBER CONSUMED. Anchor DEC next 0019, ADR next
0011, CL next 31, WS-E next 77. Divergences 0.

========================================================================
ENTRY 2 - POST-MERGE CLOSE-OUT (2026-08-19T19:36:15Z)
========================================================================

STATUS=MERGED-VERIFIED-CLEANDOWN-COMPLETE-TREE-CLEAN

Supersedes entry 1's STATUS. Appended, not overwritten, per A4.5.

MERGE, AS READ FROM GITHUB, NOT ASSERTED:
  PR 152 state MERGED. mergedAt 2026-08-19T19:34:43Z.
  Merge commit 574cd7b.

CHECKS: ONE RAN, NOT TWO. structural-checks SUCCESS; the lint-test workflow
did not appear. Paths filter behaving as designed for a docs-only change,
and the correct gate for it - but "checks green" here rests on ONE
workflow. Recorded rather than rounded, the same way entry 2 of the
PROMPT 150 return leg recorded it, so the two are comparable.

POST-MERGE VERIFICATION, run from main after fast-forward:
  Fast-forward 5a8121c..574cd7b reported 2 files, +131 - IDENTICAL to the
    pre-merge branch figures in entry 1. The merge changed nothing on the
    way in.
  Tree CLEAN. Status with untracked-all returned empty on merged main.
  check_docs on merged main - "Checked 176 markdown files. No findings."
  lint on merged main - "All checks passed!"
  ANCHOR RE-READ ON MERGED MAIN - WS-E next 77, CL next 31, DEC next 0019,
    ADR next 0011, divergences 0. UNMOVED IN MERGED STATE, which is the
    verification that matters for this act specifically: the whole
    numbering treatment was that no number is consumed, and it is now
    evidenced on main rather than only on a branch.
  CONTENT VERIFIED, not merely line counts: all three edits located by
    content search on merged main - the WS-E 76 amendment heading at
    register line 1373, the WS-E 73 discharge-note heading at register line
    1069, and the third-coverage-fact block at line 1746 of the project
    instructions file.

CLEANDOWN: local branch deleted with the SAFE delete, which refuses unless
merged. Reported "Deleted branch governance/guard-thread-close-out (was
e299d42)". Remote head pruned by fetch. Branch listing shows only main at
574cd7b; working tree clean with untracked files included.

PERMISSION-ASK LEDGER - ENTRY 2 PHASES. Same method, same limit, HELD.
  MERGE READ: one gh read. NO DENIALS OBSERVED.
  SO-1 VERIFY: switch, pull, status, manifest regeneration, anchor read,
    check_docs, lint, content search. NO DENIALS OBSERVED.
  CLEANDOWN: safe branch delete, fetch with prune, branch listing. NO
    DENIALS OBSERVED.
    >> NO CLAIM IS MADE ABOUT WHETHER AN ASK SURFACED ON THE DELETE. On
    >> three prior attested windows an ask did surface on this exact
    >> command form; that is the operator's record, not this one's, and
    >> the executor-side entry stays silent on the question.

OPERATOR HALF OF THE ASK LEDGER FOR THIS RUN: OWED. Its absence is written
rather than silent.

WHAT PROMPT 153 ASKED FOR AND WHAT IT GOT: boot re-run with both
precondition limbs checked against a freshly regenerated anchor; the
numbering convention read, cited and verified mechanically rather than
invented; all three edits landed with every required element, including the
two constraints that most needed holding - the settings-path caveat and the
inference-labelling of the historical unification; single commit; battery
with pytest's absence reasoned; PR opened and STOPPED at PR-open; then
merged and closed out. Nothing in the item was left undone, and nothing
outside its scope was touched. One thing it did not ask for is recorded
rather than omitted: the placeholder error made and corrected within the
act.

REGISTERS AT CLOSE: NO NUMBER CONSUMED BY THIS ACT, verified in merged
state. Anchor DEC next 0019, ADR next 0011, CL next 31, WS-E next 77.
Divergences 0.

ARC A-2026-08-19-03 REMAINS OPEN at the time of writing; session close
follows separately.

STILL OWED:
  (a) the safe-delete-ask question, with its recommended read-only
      disposition;
  (b) the structural-unification decision for WS-E 73 and 76;
  (c) the operator half of this run's ask ledger.

- Entries 1 and 2 staged by CC. Single CC instance; CC-ID omitted per A4.1.
  The terminal readback is corroboration; this draft is the return leg of
  record.
- Dotted filenames avoided throughout per extended F-R1.
```

---

## Entry 3 — the fold: operator half of the ask ledger (appended 2026-08-21)

**A repository act under PROMPT 132R, appended per the A4.5 convention: entries
1 and 2 above are never overwritten.** This entry discharges the obligation
entry 2 recorded against itself — item (c) of its STILL OWED list, "the operator
half of this run's ask ledger", whose absence entry 2 deliberately wrote down
rather than passing over in silence.

**--- OPERATOR HALF, verbatim (Chair, coordinator chat, 19 Aug 2026) ---**

> "Operator ask-ledger halves: PROMPT 152 - none fired. PROMPT 153 -
> none fired."

**--- Coordinator note carried with it (ARCA-D-OPERATOR-HALVES-152-153):**

> PROMPT 152: executor observed exactly one denial (Q4, predicted,
> verbatim deny) and zero asks surfaced to the operator. PROMPT 153: a
> register-write PR ran branch-to-PR-open with zero asks surfaced.
> Both runs now carry complete two-half evidence. ---

**What this closes for this record, precisely.** Entry 1 section 5 and entry 2's
phase ledger both recorded NO DENIALS OBSERVED across every phase — boot,
convention read, the three edits, commit-message authoring, battery, commit,
push, PR, merge read, post-merge verification and cleandown — while holding the
method's limit in view throughout: *"No denials observed" NEVER means "no ask
fired"*. The operator half now supplies the other side: **no ask fired**. A
register-write PR ran branch-to-PR-open with zero asks surfaced, which is the
strongest single piece of Amendment 5 acceptance evidence this record carries,
and it could only ever have come from the operator.

**It also answers, in the negative, the one question entry 2 expressly refused
to answer.** Entry 2's cleandown ledger says of the safe branch delete: *"NO
CLAIM IS MADE ABOUT WHETHER AN ASK SURFACED ON THE DELETE. On three prior
attested windows an ask did surface on this exact command form; that is the
operator's record, not this one's."* The operator record for this run is now in
hand and reports none fired. **That does not settle item (a), the
safe-delete-ask question**, which asks *why* the ask fires and is not answered
by one run in which it did not; it removes this run from the tally of windows in
which it did.

**What this fold does not close.** The completion is of this run's ledger, not
of WS-E 75. That entry records a structural gap — no vantage point both triggers
and observes a live ask — and an operator reporting what their screen showed is
the workaround that gap requires, not evidence against it. Items (a) and (b) of
the STILL OWED list stand untouched.
